<UserControl x:Class="CRMS.Views.Support.TicketListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CRMS.Views.Support"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Margin="10" Background="Transparent" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Список заявок" FontSize="16" Margin="0,0,0,20" Foreground="White" FontWeight="Bold"/>

            <ListView Grid.Row="1" Margin="10" Name="TicketsList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Номер" Width="60" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Тема" Width="200" DisplayMemberBinding="{Binding Title}"/>
                        <GridViewColumn Header="Очередь" Width="150" DisplayMemberBinding="{Binding Queue}"/>
                        <GridViewColumn Header="Приоритет" Width="200" DisplayMemberBinding="{Binding Priority}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Border>
</UserControl>
