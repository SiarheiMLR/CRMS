<UserControl x:Class="CRMS.Views.Support.ClosedTicketsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CRMS.Views.Support"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <local:TicketFiltersControl Grid.Column="0" Height="Auto" Width="Auto" Margin="0,0,10,0" DataContext="{Binding}"/>
        <local:TicketListControl Grid.Column="1" Height="Auto" Width="Auto" Margin="0,0,10,0" DataContext="{Binding}"/>
        <local:TicketDetailsControl Grid.Column="2" Height="Auto" Width="Auto" Margin="0,0,10,0" DataContext="{Binding}"/>
    </Grid>
</UserControl>
