<UserControl x:Class="CRMS.Views.Support.AddCommentDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:local="clr-namespace:CRMS.Views.Support"
             xmlns:userControls="clr-namespace:CRMS.Views.User"
             xmlns:viewModels="clr-namespace:CRMS.ViewModels.Support"
             xmlns:converters="clr-namespace:CRMS.Infrastructure.Converters"
             xmlns:domain="clr-namespace:CRMS.Domain.Entities;assembly=CRMS.Domain"
             mc:Ignorable="d"             
             Height="Auto" Width="Auto"
             HorizontalAlignment="Center" VerticalAlignment="Center">

    <UserControl.Resources>
        <!-- Стиль для принудительного черного цвета текста в RichTextBox -->
        <Style TargetType="RichTextBox">            
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </UserControl.Resources>

    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6C5B7B" Offset="0"/>
            <GradientStop Color="#FFC06C84" Offset="0.5"/>
            <GradientStop Color="#FF355C7D" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок с привязкой к номеру заявки -->
        <TextBlock Grid.Row="0"
                   Text="{Binding SelectedTicket.TicketNumber, StringFormat='Комментарий к заявке {0}'}"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <!-- Подключаем готовый RichTextEditor с привязкой документа и команды сохранения -->
        <Border Grid.Row="1"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="5"
                Background="Transparent"
                Padding="5">
            <userControls:RichTextEditor x:Name="CommentEditor" 
                                         Document="{Binding NewCommentDocument, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         SaveCommand="{Binding SaveCommand}"/>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">

            <Button Content="Добавить"
                    Command="{Binding AddCommentConfirmCommand}"
                    CommandParameter="{Binding ElementName=CommentEditor}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Foreground="White"
                    Margin="0 0 20 0"/>

            <Button Content="Отмена"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="TicketDetailsDialog"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Foreground="{DynamicResource PrimaryHueMidBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>