<UserControl x:Class="CRMS.Views.User.ViewCommentDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:local="clr-namespace:CRMS.Views.User"
             xmlns:viewModels="clr-namespace:CRMS.ViewModels.UserVM"
             xmlns:converters="clr-namespace:CRMS.Infrastructure.Converters"
             xmlns:domain="clr-namespace:CRMS.Domain.Entities;assembly=CRMS.Domain"
             mc:Ignorable="d"             
             Height="Auto" Width="Auto">

    <UserControl.Resources>        
        <converters:XamlToFlowDocumentConverter x:Key="XamlToFlowDocumentConverter"/>     
    </UserControl.Resources>

    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6C5B7B" Offset="0"/>
            <GradientStop Color="#FFC06C84" Offset="0.5"/>
            <GradientStop Color="#FF355C7D" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="{Binding SelectedTicket.TicketNumber, StringFormat='Комментарии к заявке {0}'}"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <!-- Список комментариев -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding SelectedTicket.Comments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type domain:TicketComment}">
                        <Border CornerRadius="8"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                Background="#333333"
                                Padding="15"
                                Margin="10,0,2,10">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding User.DisplayName}" 
                                               FontWeight="Bold" 
                                               Foreground="LightBlue"
                                               Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Created, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}"
                                               FontSize="12"
                                               Foreground="LightGray"
                                               FontStyle="Italic"/>
                                </StackPanel>
                                <Border Margin="0,5,0,0"
                                        BorderThickness="1"
                                        Padding="10"
                                        Background="#20FFFFFF"
                                        BorderBrush="LightGray"
                                        CornerRadius="5">
                                    <FlowDocumentScrollViewer 
                                            Document="{Binding Content, Converter={StaticResource XamlToFlowDocumentConverter}}"
                                            Foreground="White"
                                            IsToolBarVisible="False"
                                            VerticalScrollBarVisibility="Auto"
                                            MaxHeight="300"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">

            <Button Content="Закрыть"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Foreground="White"
                    Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
